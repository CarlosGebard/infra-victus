name: Prepare deploy context
description: Prepare SSH material and Ansible inventory for deploy jobs.

runs:
  using: composite
  steps:
    - name: Prepare SSH material
      shell: bash
      run: bash .github/scripts/prepare-ssh.sh

    - name: Build inventory
      shell: bash
      run: bash .github/scripts/build-deploy-inventory.sh
