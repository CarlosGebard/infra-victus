services:
  backstage:
    ports:
      - "${BACKSTAGE_BIND_IP:-127.0.0.1}:${BACKSTAGE_HTTP_PORT:-7007}:7007"

  backstage-database:
    volumes:
      - ../../.tmp/backstage/postgres:/var/lib/postgresql/data
