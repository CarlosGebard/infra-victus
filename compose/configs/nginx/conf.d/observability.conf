resolver 127.0.0.11:53 valid=10s;
resolver_timeout 5s;

upstream grafana_upstream {
  server grafana:3000 resolve;
}

upstream prometheus_upstream {
  server prometheus:9090 resolve;
}

upstream loki_upstream {
  server loki:3100 resolve;
}
